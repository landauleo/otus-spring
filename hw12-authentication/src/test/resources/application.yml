spring:
  mongodb:
    embedded:
      version: 4.0.2
  main:
    allow-circular-references: true
  data:
    mongodb:
      database: book
  sql:
    init:
      mode: always
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  enabled: false